apiVersion: v1
kind: Service
metadata:
  name: {{ include "distcc.fullname" . }}
  labels:
    {{- include "distcc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "distcc.selectorLabels" . | nindent 4 }}
